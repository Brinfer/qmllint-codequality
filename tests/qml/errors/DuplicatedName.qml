import QtQuick

Item {
    Rectangle {
        signal duplicateSignal
        signal duplicateSignal
    }

    Rectangle {
        property int duplicateProperty
        property int duplicateProperty
    }
}